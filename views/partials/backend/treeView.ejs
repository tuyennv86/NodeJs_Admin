<% if (items.length) { %>
<ul>
<% } %>

    <% items.forEach(function(item){ %>
    <li>
    <% var space = "";
     for(var j = 1 ;j< level; j++ ){
            space = space + "--";
    } %>
        <a href="<%= item._id %>"> <%= space %> <%= item.categoryName %></a>         
        <%- include('treeView', {items: item.children, level: level + 1}) %>       
    </li>
    <% }) %>

<% if (items.length) { %>
</ul>
<% } %>